# Minimal dependencies for Lambda market data fetcher
# Optimized to avoid conflicts and reduce package size

# Core dependencies only
yfinance==0.2.28
boto3==1.34.0

# Compatible versions (avoid conflicts)
pandas==2.0.3
numpy==1.24.3
requests==2.31.0
lxml==4.9.3
html5lib==1.1
